# Assets

This directory is reserved for project assets such as icons, model
resources, or UI resources.

There are currently no additional assets beyond the source code and the
demo images in `output/`. The fine-tuned TrOCR model used for handwriting
recognition is **not** stored here — see
[`docs/installation.md`](../docs/installation.md) for how it is configured.
